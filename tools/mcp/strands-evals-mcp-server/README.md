# Strands Evals MCP Server

An MCP server that exposes the Strands Evals SDK capabilities:
- **ActorSimulator**: For multi-turn evaluations.
- **ExperimentGenerator**: For trace-level evaluations.

## Running the Server

```bash
uvx --from . strands-evals-mcp-server
```
